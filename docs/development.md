# Разработка Agent System

# agent-system

Общий набор агентов, скиллов и рабочего состояния, который одинаково работает
из Claude Code и из Codex — по одному харнессу на сессию, с переключением между ними
без ручного пересказа контекста.

Полный дизайн: [docs/design.md](docs/design.md).
Операционный контракт, который читают оба харнесса: [AGENTS.md](AGENTS.md).

## Установка

```bash
./setup.sh
```

Скрипт идемпотентный. Он:

1. создаёт обычную папку `~/.agents-memory/`, без общей `.git`;
2. создаёт отдельный Git-репозиторий `<key>/` для памяти проекта;
3. проставляет симлинки `.agents/memory` и `.claude/skills`.

Запускать **в каждом worktree и на каждой машине** — симлинки gitignored и между
рабочими деревьями не разделяются.

Ключ проекта — имя подкаталога в хранилище. Он **обязан совпадать у всех worktrees
одного репозитория**, иначе деревья видят разную память, притом что симлинки на месте
и ошибки нет. Имя *текущего* каталога ключом быть не может: у worktree оно другое
по построению.

`setup.sh` выбирает и **запоминает** оба значения:

| | приоритет |
|---|---|
| ключ | явный аргумент → сохранённый `agents.memoryKey` → существующая ссылка → имя каталога основного рабочего дерева git + `-memory` |
| хранилище | `AGENTS_MEMORY_STORE` → сохранённый `agents.memoryStore` → существующая ссылка → `~/.agents-memory` |

Сохраняются они в общий локальный git config (`git config --local`; ни global,
ни `config.worktree` для них не используются), который у всех worktrees один — поэтому
значения переживают и повторный запуск без переменной, и переименование каталога
репозитория.

Вне git сохранять негде: скрипт предупреждает и берёт имя текущего каталога + `-memory`, а явный
ключ приходится передавать каждый раз. Свежий клон git config не получает — задайте
ключ один раз через `./setup.sh <project>`.

Явная смена ключа или хранилища перенастраивает **только текущее дерево**; в остальных
нужно повторить `./setup.sh`. Отставшие деревья находит `python3 tools/check_state.py`
и сообщает о них ошибкой. Содержимое старой памяти автоматически не переносится.

Перенос памяти на другую машину:

```bash
cd ~/.agents-memory/<key> && git remote add origin <приватный-remote> && git push -u origin main
# на новой машине:
git clone <приватный-remote> ~/.agents-memory/<key>
```

## Что здесь есть

```
.agents/agents/     три роли: explorer, implementer, reviewer  (canonical)
.agents/skills/     локальные скиллы агента для разработки CLI
skills/             поставляемые скиллы → .agents/skills/ целевого проекта
.agents/state/      tasks/<slug>/task.md + journal/<ts>-<sid8>.md  (в git)
                    sessions/<session-id>  привязка чата к задаче   (gitignored)
tools/              генератор, адаптеры, валидатор состояния, тесты
.claude/agents/     GENERATED
.codex/agents/      GENERATED
```

## Команды

```bash
python3 tools/gen_agents.py           # сгенерировать нативные определения
python3 tools/gen_agents.py --check   # drift-проверка, ничего не меняет (для CI)
python3 tools/check_state.py          # проверить состояние задач, привязки и память
python3 -m unittest discover tools/tests -v
```

Генератор падает с объяснением, если каноническую границу доступа нельзя выразить
средствами харнесса — вместо того чтобы выдать конфигурацию с более широкими правами,
чем объявлено. Это его главное свойство, см. `docs/harness-differences.md`.

## Статус

Реализованы этапы 0–7 и 9 из §19 дизайна: состояние, скиллы, роли, генератор
с адаптерами, drift-проверка, валидатор состояния, 44 теста, CI.

**Этап 8 — complexity gate и fix loop — реализован как политика в `AGENTS.md`,
но не обкатан.** Кодом это и не закрывается: чтобы узнать, срабатывает ли gate там,
где нужно, и находит ли reviewer что-нибудь на самом деле, нужны прогоны на реальных
задачах. Что для этого собирать — `docs/telemetry.md`.

### Требует сверки

- **Имена моделей Codex** написаны по документации и не проверялись запуском. Помечено
  `VERIFY` в `tools/adapters/codex.py`. Если они окажутся другими, правится только
  адаптер — канона это не касается.
- **Таблица уровней `effort` по моделям Claude** написана по документации и не
  проверялась запуском (`VERIFY` в `tools/adapters/claude.py`). Модель, которой в таблице
  нет, считается неизвестной: `effort` переносится, генератор предупреждает.
- **explorer и reviewer лишены `shell`**, чтобы граница read-only была настоящей.
  Цена: reviewer не может запустить тесты, только читать код.
- **Ни одна из ролей ещё не запускалась в бою.** Зелёные тесты подтверждают, что
  генератор делает то, чего от него ждут тесты, — не то, что харнессы принимают его
  вывод. Ближайшая проверка: прогон трёх ролей в обоих харнессах и сценарий прерывания
  задачи с продолжением в другом харнессе.


# Память проекта — симлинк на внешнее хранилище (§2.1)
.agents/memory

# Локальное состояние рабочего дерева, не переносимое (§5, §11)
.agents/state/ACTIVE
.agents/state/LOCK

# Локальная телеметрия, никогда не каноническое состояние (§17)
.agents/runs.jsonl

.DS_Store
__pycache__/
